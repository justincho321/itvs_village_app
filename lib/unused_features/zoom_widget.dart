//currently not working. However, Zoom feature is proven to work in separate project

/*
import 'package:flutter/material.dart';
import 'meeting_screen.dart';

class ZoomApp extends StatelessWidget {
  const ZoomApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Example Zoom SDK',
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
      navigatorObservers: const [],
      debugShowCheckedModeBanner: false,
      initialRoute: '/',
      routes: {
        '/': (context) => const MeetingWidget(),
      },
    );
  }
}
*/
